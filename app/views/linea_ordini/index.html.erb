<p id="notice"><%= notice %></p>

<h1>Linea Ordini</h1>

<table>
  <thead>
    <tr>
      <th>| Prodotto |</th>
      <th>| Carrello | </th>
      <th>| Quantità |</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @linea_ordini.each do |linea_ordine| %>
      <tr>
        <td><%= linea_ordine.prodotto_id %></td>
        <td><%= linea_ordine.carrello_id %></td>
        <td> <%= linea_ordine.qta %> </td>
        <td><%= link_to 'Show', linea_ordine %></td>
        <td><%= link_to 'Edit', edit_linea_ordine_path(linea_ordine) %></td>
        <td><%= link_to 'Destroy', linea_ordine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Linea Ordine', new_linea_ordine_path %>
